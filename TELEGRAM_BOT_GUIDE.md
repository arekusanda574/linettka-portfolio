# 🤖 Інструкція запуску Telegram-Бота для додавання фото у Портфоліо

Цей бот дозволяє замовнику (Ліні) завантажувати нові фотографії прямо зі смартфона у Telegram. Бот самостійно:
1. Запитує категорію (*Студія 📷*, *Заобрій 🌿*, *Пари 👩‍❤️‍👨*, *Події 🕊️*).
2. Оптимізує фото в ультра-швидкий WebP формат (max 1600px).
3. Створює мініатюру (thumb max 600px).
4. Автоматично вставляє фото у `index.html` на початок відповідної категорії.

---

## 🚀 Покроковий запуск за 1 хвилину

### Крок 1: Створення бота в Telegram
1. Відкрийте Telegram та знайдіть офіційного бота **`@BotFather`**.
2. Надішліть команду `/newbot`.
3. Вкажіть ім'я бота (наприклад `linettka portfolio bot`) та username (наприклад `linettka_portfolio_bot`).
4. Скопіюйте отриманий токен (наприклад `7123456789:AAEF...`).

### Крок 2: Запуск бота на сервері/комп'ютері
Надішліть у терміналі команду з вашим токеном:

```bash
BOT_TOKEN="ВАШ_ТОКЕН_ВІД_BOTFATHER" python3 bot.py
```

---

## 📱 Як користуватися фотографу у Telegram:

1. Відкрийте бот у Telegram та натисніть `/start`.
2. Надішліть будь-яку фотографію.
3. Бот миттєво відповість кнопками:
   - `Студія 📷`
   - `Заобрій 🌿`
   - `Пари 👩‍❤️‍👨`
   - `Події 🕊️`
4. Натисніть потрібну категорію — і фотографія **автоматично оптимізується та з'явиться на сайті у портфоліо!** 🎉

---

## 🌐 Як це працює з хостингом Netlify (Автоматичний Деплой):

Оскільки сайт підключений до **Netlify**, нові фотографії повинні потрапляти у ваш GitHub-репозиторій, щоб Netlify опублікував їх на живому сайті:

1. **Авто-деплой з бота:**  
   Я впровадив у `bot.py` автоматичний виклик `git commit & push`. Щоразу, коли Ліна додає або видаляє фото через Telegram-бот, бот самостійно відправляє оновлений код у ваш Git-репозиторій.
2. **Миттєве оновлення Netlify:**  
   Netlify підхоплює цей `push` і за **5–10 секунд автоматично оновлює живий сайт у всьому світі!** 🎉
